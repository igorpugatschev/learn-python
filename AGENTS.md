# Repository Instructions

## Git Workflow

- After completing any requested change, automatically create a local Git commit for the files changed by that task.
- Keep unrelated or unfinished user changes out of the commit.
- Do not push automatically. Push only when the user explicitly requests it; otherwise the user may push independently.
- Verify the relevant changes before committing and report the commit hash in the final response.
